const devApiHost = 'https://api.sino-otm.com';

export const devConfig = {
  host: `${devApiHost}/japi`,
  mall_host: 'https://apitestv3.mekoomall.com/mallapi_uat',
  net_host: `${devApiHost}/napi`,
};
